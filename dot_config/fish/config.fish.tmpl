source {{ .chezmoi.pathSeparator }}usr{{ .chezmoi.pathSeparator }}share{{ .chezmoi.pathSeparator }}{{ .chezmoi.osRelease.id }}-fish-config{{ .chezmoi.pathSeparator }}{{ .chezmoi.osRelease.id }}-config.fish

# overwrite greeting
# potentially disabling fastfetch
#function fish_greeting
#    # smth smth
#end

starship init fish | source 	# Tema

zoxide init fish | source	# Zoxide

set -gx SSH_AUTH_SOCK "$XDG_RUNTIME_DIR{{ .chezmoi.pathSeparator }}gcr{{ .chezmoi.pathSeparator }}ssh"	# Valent

# bun
set --export BUN_INSTALL "$HOME{{ .chezmoi.pathSeparator }}.bun"
set --export PATH $BUN_INSTALL{{ .chezmoi.pathSeparator }}bin $PATH
